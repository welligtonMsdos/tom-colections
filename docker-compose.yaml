services:
  frontend:
    build: .
    container_name: tomshow_frontend
    ports:
      #- "80:8080"
      - "4200:4200"
    networks:
      - tom-auth-net
    volumes:
      - .:/app
      - /app/node_modules

networks:
  tom-auth-net:
    driver: bridge
